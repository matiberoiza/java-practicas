# Contexto del estudiante

Soy **Matias Beroiza**, estudiante de Ingeniería en Informática en **Duoc UC**, mención Inteligencia Artificial (currículum 1446111). Estoy comenzando el 2° semestre del 1er año. Uso Mac. Mi GitHub es `matiberoiza` y mi repositorio de prácticas es `java-practicas`. Estoy migrando de VS Code a **IntelliJ IDEA**.

Mi ramo actual es **DSY1102 — Desarrollo Orientado a Objetos**. La bibliografía obligatoria es Deitel & Deitel *"Java Como Programar"* (10ª ed.) como principal, y Barnes *"POO con BlueJ"* como complemento.

---

## Cómo quiero que me enseñes

- Quiero construir una **base muy sólida** en Java, no ir rápido.
- Prefiero que me hagas **preguntas para verificar que entendí** antes de avanzar al siguiente tema, en vez de solo explicarme.
- Cuando cometa un error en el código, explícame **por qué** está mal y qué principio hay detrás, no solo me des la corrección.
- Si escribo algo que funciona pero **no es la mejor práctica**, dímelo.

---

## Lo que ya domino en Java

**Fundamentos**
- Variables y tipos primitivos (`int`, `double`, `boolean`, `String`, `char`)
- Operadores aritméticos, de comparación y lógicos (`&&`, `||`)
- Estructuras de control: `if/else`, `while`, `for`, `switch`
- Métodos con parámetros y retorno · `void` vs tipo de retorno

**Programación Orientada a Objetos**
- Clases y objetos · constructores · `this`
- Encapsulación (`private` + getters/setters con validación)
- Herencia (`extends`, `super`)
- `@Override` y sobrescritura de métodos
- `instanceof` y casting
- Composición (una clase con `ArrayList` de otra adentro)
- Clases abstractas (`abstract class` y métodos abstractos)
- Interfaces (`interface` e `implements`)

**Herramientas y utilidades**
- `Scanner` con manejo correcto del buffer (`nextLine()` después de `nextInt()`)
- `ArrayList` y recorrido con `for-each`
- Operador ternario
- Text blocks (`"""`)
- `.trim()`, `.toLowerCase()`, `.contains()`, `.isEmpty()`
- `throw new IllegalArgumentException()`

---

## Ejercicios que ya completé

| Ejercicio | Conceptos aplicados |
|---|---|
| `es_par()` | Métodos, tipos de retorno, boolean |
| Clase `Libro` | Encapsulación, constructores, `this` |
| Sistema de restaurante | Herencia (Empleado / Garzon / Chef), `@Override` |
| Veterinaria | Herencia (Animal / Perro / Gato), Scanner, ArrayList |
| Tienda de videojuegos | `instanceof`, casting, `throw` |
| Biblioteca | Menú interactivo con `switch` y `while`, búsqueda con `.contains()` |
| Sistema de cine | Clase abstracta, interfaz `Calificable`, composición (`Sala` con `ArrayList<Pelicula>`) |

---

## Lo que me falta aprender

- [ ] **Polimorfismo formal** — lo he usado pero sin consolidar la teoría
- [ ] **Manejo de excepciones completo** con `try/catch/finally`
- [ ] **Sobrecarga de métodos**
- [ ] **Diagramas de clase UML**

---

## Otras áreas

**Bases de datos (Oracle)**
- Herramientas instaladas: Oracle Data Modeler, SQL Developer, Oracle XE
- CRUD completo: `INSERT`, `SELECT`, `UPDATE`, `DELETE`
- Filtros: `WHERE`, `AND`, `OR`, `LIKE`, `ORDER BY`
- Modelo entidad-relación completo con tabla intermedia N:M
- **Pendiente:** `JOIN`, JDBC (conectar Java con base de datos)

**Git y GitHub**
- Flujo completo: `init`, `add`, `commit`, `push`, `pull`, `log`, `status`, `diff`
- Ramas: `checkout -b`, `merge`, `branch -d`
- Resolución de conflictos de merge
- `.gitignore` configurado correctamente
